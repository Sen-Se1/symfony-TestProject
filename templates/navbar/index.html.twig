<nav class="navbar navbar-expand-lg bg-secondary mb-4 p-2">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_index') }}">LOGO</a>
		<div class="collapse navbar-collapse d-flex justify-content-center fs-5 p-1" id="navbarNavAltMarkup">
			<div class="navbar-nav align-items-center">
				<a class="nav-link active" aria-current="page" href="{{ path('app_index') }}">Home</a> 

				{% if app.user %}
					<a class="nav-link active" href="{{ path('show_posts') }}">Show Posts</a> 
					<a class="nav-link active" href="{{ path('create_post') }}">Create Post</a> 
					<a class="nav-link active" href="{{ path('app_logout') }}">Logout</a>
				{% endif %}
				{% if not app.user %}
					<a class="nav-link active" href="{{ path('app_login') }}">Login</a> 
					<a class="nav-link active" href="{{ path('app_register') }}">Register</a> 
				{% endif %}
				
			</div>
		</div>
	</div>
</nav>